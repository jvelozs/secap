<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Consulta de Cédulas</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
  <div class="container mx-auto p-6">
    <h1 class="text-3xl text-center mb-6 text-blue-600">Consulta de Cédulas</h1>

    <div class="bg-white shadow-md rounded-lg p-8 max-w-md mx-auto">
      <form action="/buscar_cedula" method="POST">
        <label for="cedula" class="block text-lg font-medium text-gray-700 mb-2">Ingrese la cédula</label>
        <input type="text" name="cedula" id="cedula" maxlength="10" required class="w-full p-3 border border-gray-300 rounded-lg mb-4" placeholder="Número de cédula">
        <button type="submit" class="w-full bg-blue-500 text-white py-3 rounded-lg hover:bg-blue-600 transition duration-300">Buscar</button>
      </form>

      <% if (typeof result !== 'undefined') { %>
        <div class="mt-6 bg-blue-50 p-4 rounded-lg shadow-md">
          <% if (result.error) { %>
            <p class="text-red-600"><%= result.error %></p>
          <% } else { %>
            <h2 class="text-xl font-bold text-blue-600 mb-4">Resultado:</h2>
            <p><strong>Apellidos:</strong> <%= result.apellidos %></p>
            <p><strong>Nombres:</strong> <%= result.nombres %></p>
            <p><strong>Fecha de Nacimiento:</strong> <%= result.fecha_nacimiento %></p>
            <p><strong>Estado Civil:</strong> <%= result.estado_civil %></p>
          <% } %>
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>

